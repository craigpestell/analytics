import Analytics, {
  PageViewProps,
  type PageParams,
  type TrackParams,
} from './lib/analytics';

export { Analytics, type PageViewProps, type PageParams, type TrackParams };

export default Analytics;
